---
id: DOC-###
type:
status: draft
phase: null
owner: james
tags: []
links: []
updated: YYYY-MM-DD
---

# <Title>

<!-- The H1 is what the board shows as this doc's title. Make it a real title, not a
     filename. The viewer strips fenced code blocks before matching, so a `#` comment
     inside a bash block won't be mistaken for it.

     Pick `type` from the table in README-DOCS.md §3 — it decides which board section
     this lands in. Leaving it blank means the doc is invisible.

     Pick `tags` from the 14-tag vocabulary in §4. Never invent one. -->

<!-- One or two sentences: what this doc is for and who should read it.
     If you can't write that, the doc probably doesn't have a job yet. -->

## <Section>

<!-- Guidance for the writer goes in HTML comments like this one — the reader can
     delete them once the section is filled. Prefer headers + prompts over
     placeholder prose: an empty section is honest, invented content is not.

     Where you don't know something, write it as a TODO addressed to a person:
       <!-- TODO(james): where do uploaded photos physically live? -->
     rather than guessing. A clearly-marked gap is worth more than a plausible
     wrong answer. -->

## <Section>

---

<!-- If this doc is superseded, don't delete it: set status: deprecated and add the
     replacement's id to `links`. The history is worth keeping. -->
